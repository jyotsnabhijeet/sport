server:
  port: '8080'
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/sportyshoes
    username: root
    password: sumant
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
      hibernate:
        ddl-auto: update
       